# Node.js Standalone Server Configuration
# Copy this to .env and customize for your deployment

# Authentication key for creating/editing pastes (required)
AUTH_KEY=your-secret-key-here

# Optional: Custom database path (defaults to ./data/pbnj.db)
# DATABASE_PATH=/path/to/pbnj.db

# Optional: Server port (defaults to 4321)
# PORT=4321

# Optional: Host binding (defaults to localhost, use 0.0.0.0 for containers)
# HOST=0.0.0.0
